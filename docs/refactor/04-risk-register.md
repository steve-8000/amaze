# 04 — Risk Register (concrete, ranked)

> Lane E / Phase 0. The original plan's §16 risk register is not present in this
> repo (only `PARALLEL-WORKPLAN.md`, §0–§8, is checked in). This register is
> therefore **derived** from the workplan's stated invariants (§0 "핵심 원칙",
> §7 common rules, §8 "가장 위험한 레인은 C2와 L") and grounded in the concrete
> files discovered in docs 00–03. Ranked highest → lowest residual risk.
> Paths relative to `packages/coding-agent/src/`.

| # | Risk | Concrete files | Lane | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | **Execution-owner promotion drops budget accounting.** `GoalRuntime` owns token+wall-clock accounting (`runtime.ts:554` `#flushUsageLocked`, `:600` `flushUsage`, `:829` `addExternalUsage`, `:403` `#withAccounting` serialization). Mission types have **no budget fields** (`types.ts` has none). Moving ownership to `MissionRuntime` risks double-count, lost subagent cost (`task/index.ts:1553`), or broken budget-limit steer. | `goals/runtime.ts`, `mission/runtime.ts`, `mission/store.ts`, `task/index.ts:1553` | C2 | Med-High | High | Keep `GoalRuntime` live (workplan §0, §7.4); add `MissionBudget` (Lane A) as new column/side-table; port accounting tests verbatim; C2 single-merge + regression (§8). |
| R2 | **Closing-audit verifier behavior change.** `completeGoalFromTool` runs `AcceptanceVerifier` BEFORE status flip and throws `GoalAcceptanceFailureError` on fail (`runtime.ts:745`,`:759`). Workplan demands "Verifier 동작 동일 유지". A migrated runtime that records mission verification but skips the blocking throw silently regresses gate. | `goals/runtime.ts:713`, `goals/verifier.ts`, `goals/llm-judge.ts` | C2/F/L | Med | High | Preserve the throw path; reuse `test/goals/closing-audit-e2e`, `uncertain-policy`; do not move verifier logic until L, and only as delegation. |
| R3 | **`goals/runtime.ts` write-ownership conflict between Lane F and Lane L.** Both lanes modify the same 988-line file; the mission seam (`runtime.ts:33`,`:773`) is the shared edit zone. Parallel edits → merge conflicts / lost dual-write. | `goals/runtime.ts` (F + L both own) | F, L | Med | Med-High | §1.2/§7.6 serialize: **L only after F + C2 + K merge**. F extends the existing seam; L reduces to thin wrapper afterward. |
| R4 | **Prompt-cache thrash from `renderGoalBlock` changes.** `renderGoalBlock` (`runtime.ts:256`) is documented byte-stable for cache stability. Adding `renderMissionBlock` or editing the goal block risks cache invalidation on every turn. | `goals/runtime.ts:256`, `session/agent-session.ts:3951` (`buildActivePrompt`) | F | Med | Med | §7.5: never remove `renderGoalBlock`; add `renderMissionBlock` in parallel; keep byte-stable ordering; assert via existing goal-events / prompt tests. |
| R5 | **Event-log v2 breaks v1 parsing.** `mission/events.ts:117` `MissionEvent` union lacks lifecycle events; adding v2 envelope risks breaking the single JSONL audit log (`jsonl-sink.ts`) and read-model parsing (`read-model.ts`, `projection.ts`). | `mission/events.ts`, `mission/jsonl-sink.ts`, `mission/read-model.ts`, `mission/projection.ts` | B | Med | Med-High | §2 Lane B: dual-read v1/v2; keep single audit log (workplan §0); regression `test/mission/producers.test.ts`, `read-model.test.ts`. |
| R6 | **Mutation tools escape the gateway.** Tools dispatch generically (`agent-session.ts:3193/3205/3262`) AND via the subprocess registry (`task/subprocess-tool-registry.ts:65`). Direct-exec sites (`tools/recipe/runners/*`, `tools/cua.ts`, `tools/browser/registry.ts`, `goals/runtime.ts:77`) and URL fetch (`read.ts:1484`) bypass a `bash`-only gateway. | `tools/index.ts:311`+, `session/agent-session.ts`, `task/subprocess-tool-registry.ts`, `tools/recipe/runners/*` | C1, H | Med-High | High | Gate at both dispatch surfaces + special-case §4 of doc 02; Lane H acceptance: "mutation 도구는 gateway 없이 실행 불가". |
| R7 | **Durable memory write bypasses curator.** Durable writes (`NexusStore.add` `store.ts:410`, `importSource` `:828`, `replace` `:495`, `remove` `:530`) are driven by consolidation (`pipeline.ts:84`,`:213`) via `onTurnEnd` (`nexus-backend.ts:259`). A bridge that only tags recall, not writes, lets unverified memory persist as if authoritative. | `nexus/pipeline.ts`, `nexus/store.ts`, `memory-backend/nexus-backend.ts` | D, J | Med | Med-High | Lane D durable-write rule as pure judgment fn; Lane J curator gates all §1a chokepoints; §15.5 test: curator-less durable write rejected. |
| R8 | **Memory overrides repo/instruction truth.** Authority is prose-only today (`nexus-backend.ts:125`); recall fences/sanitize (`recall-fence.ts`, `sanitize.ts`) guard injection but nothing enforces ranking. Misimplemented authority could let recalled memory outrank repo evidence. | `nexus/recall-fence.ts`, `nexus/sanitize.ts`, `memory-backend/nexus-backend.ts:125` | D, J | Low-Med | High | Lane D `authority-hierarchy` (`instruction>repo_truth>mission_evidence>verified_memory>guidance`); §15.5 test: recall does not override repo truth; mark recall `authority=guidance` in context packet. |
| R9 | **Subagent missionId/taskId binding regresses parallel/worktree execution.** `SubagentContract` (`subagent/contract.ts:29`) does not require `missionId`/`taskId`; making them mandatory risks breaking `task/parallel.ts`, `task/worktree.ts`, `task/executor.ts`. | `subagent/contract.ts`, `task/{parallel,worktree,executor}.ts` | I | Med | Med | §4 Lane I: keep parallel/worktree logic in place; add binding additively; regression `test/subagent/*`. |
| R10 | **CLI `/goal`→`/mission` alias breaks muscle-memory + help/tests.** `/goal` subcommands dispatched via `slash-commands/builtin-registry.ts` and `modes/interactive-mode.ts` (`:1880`,`:2048`); aliasing to `/mission` (`cli/mission.ts`) risks broken op mapping (set→create, drop→cancel). | `slash-commands/builtin-registry.ts`, `modes/interactive-mode.ts`, `cli/mission.ts` | K | Low-Med | Med | §5 Lane K: keep `/goal` as deprecated alias; test alias mapping + help text; depends on F + C2. |
| R11 | **`MissionStore` opened per-call with divergent dbPaths.** Many independent `new MissionStore(...)` sites (`goals/runtime.ts:40`, `task/index.ts:96/185/255`, `cli/*`, `read-model.ts:259`, `research/store.ts:890`) each open sqlite + share the event-bus singleton (`store.ts:189`). Promotion that centralizes ownership must not orphan these callers or double-attach the bus. | `mission/store.ts:181`, all `new MissionStore` sites (see doc 00 §4) | C2 | Low-Med | Med | Inventory in doc 00 §4; keep `MissionStore` constructor signature stable; route new ownership through it, not around it. |
| R12 | **Lane-ownership violation (cross-lane file edits).** §7.1 forbids writing outside a lane's owned files. Highest-contention files: `goals/runtime.ts` (F/L), `tools/index.ts` (C1 add → H replace), `mission/store.ts` + `mission/runtime.ts` (C2). | per §1.2 matrix | all | Med | Med | Enforce the §1.2 ownership matrix; DAG-ordered merges (Wave1→4); `bun run check && bun test` merge gate (§7.8). |

## Cross-cutting invariants (must hold for every lane)

From workplan §0 / §7, mapped to the files that would violate them:

- **GoalRuntime not deleted/reduced except Lane L** — guard `goals/runtime.ts`.
- **`renderGoalBlock` not removed; `renderMissionBlock` parallel only** — `goals/runtime.ts:256`.
- **Single mission audit log** — `mission/jsonl-sink.ts` stays the only sink.
- **Memory is guidance** — read fences (`nexus/recall-fence.ts`) + authority ranking (Lane D).
- **Verifier behavior identical** — `goals/verifier.ts` untouched until L.
- **Behavior-preserving Wave 1 (incl. this lane E)** — docs-only, zero source edits.

> Riskiest lanes (workplan §8 footnote, confirmed by R1/R2/R3): **C2** (execution-owner
> promotion) and **L** (legacy reduction). Both warrant solo merge + full regression.
