Native Context Optimizer implements deterministic context optimization for amaze.

It is inspired by selected context-optimization ideas from headroom:
https://github.com/chopratejas/headroom

headroom is licensed under Apache-2.0. This module is a native TypeScript
implementation for amaze and does not depend on the external headroom runtime,
proxy, Python package, or Rust extension.
