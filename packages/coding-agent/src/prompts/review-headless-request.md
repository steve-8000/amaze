## Code Review Request

### Mode

Headless review request

### Distribution Guidelines

Use the `task` tool with `agent: "checker"` and a `tasks` array.
Create exactly **1 checker task** for recent code changes.

{{#if focus}}
### Focus

{{focus}}
{{/if}}
