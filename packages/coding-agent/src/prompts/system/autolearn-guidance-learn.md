`learn` is a lightweight way to capture a reusable lesson as a managed skill: give the lesson plus a skill `name` and one-line `description`, and the lesson text becomes the `SKILL.md` body when you don't supply one. Reach for `manage_skill` instead when you want full control over the skill body, or need to delete a skill.
