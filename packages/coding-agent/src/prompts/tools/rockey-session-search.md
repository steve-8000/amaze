Search indexed past session transcripts for compact source anchors.

Use this when you need to locate where a prior discussion happened without dumping large transcript excerpts into context. Results are anchor-first and bounded. If a returned anchor looks relevant, read only that specific range.
